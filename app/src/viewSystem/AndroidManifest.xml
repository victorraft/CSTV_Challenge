<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:name=".app.CsApplication">

        <activity
            android:name=".app.ui.match_list.MatchListActivity"
            android:exported="true"
            android:theme="@style/Theme.FuzeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.ui.match_details.MatchDetailsActivity"
            android:exported="false" />
    </application>

</manifest>